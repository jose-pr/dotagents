---
name: D15
description: verification must be environment-complete: setup counts as a command
type: decision
resolves: F11
---

Env setup counts as a command (PLAN.md); Verification puts setup before checks with a 'runs top-to-bottom on a standard checkout' self-test; EXEC.md: env failures are `[!]` with the exact failing command, never improvised installs; unrun verification is 'implemented, unverified'; kb/PYTHON.md: optional-extra test modules start with `pytest.importorskip`. See [[D16]].
