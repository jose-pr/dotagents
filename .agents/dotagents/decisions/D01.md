---
name: D01
description: core+flows split: AGENTS.md keeps only every-session rules + routing table, task rules live in flows/
type: decision
resolves: F1,F4
---

`AGENTS.md` = (a) rules applying to every session + (b) load-on-demand routing table. Task-scoped rules → `flows/{PLAN,EXEC,REVIEW,REPO}.md`. Target core ≤ ~60 lines.
