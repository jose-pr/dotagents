---
name: D10
description: pre-split monolithic core superseded by flows/ + this log; backup kept untracked
type: decision
---

The old monolithic core is superseded by `flows/` + this log; its backup lives in `~/.agents` only, byte-verified post-recovery, never tracked.
