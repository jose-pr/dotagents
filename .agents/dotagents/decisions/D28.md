---
name: D28
description: ~/.agents/dotagents/ is the private working area; triage folds findings into this log
type: decision
resolves: F19
---

Private scratch (gitignored, never sanitized on write), distinct from this repo's tracked `.agents/`. Layout: `findings/YYYY-MM-DD_<slug>.md` (flat, [[D25]] capture target), `plans/<name>.md` + `plans/completed/` (private config drafts, escape hatch not default). Triage (user-requested): read findings/, fold settled ones here as entries, **move** (never delete) processed files to `findings/processed/`.
