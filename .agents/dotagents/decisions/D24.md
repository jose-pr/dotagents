---
name: D24
description: config source of truth is this repo; payload isolated under payload/, installs 1:1
type: decision
resolves: F16
supersedes: initial root-mirror layout
---

Public repo holds sanitized payload under `payload/` (installs 1:1 into `~/.agents` via `install.py`); `~/.agents` is an install target, never edited directly. Repo root has its own dev-facing `AGENTS.md`/`CLAUDE.md` so repo sessions aren't governed by the payload. Only this repo tracks `.agents/` as public docs; everything sanitized (`--repo-hygiene` mechanical, [[D22]] judgment). Superseded initial root-mirror layout. See [[D33]] (CLI now wraps install.py).
