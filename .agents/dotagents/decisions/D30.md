---
name: D30
description: live progress-marking primed at kickoff + gated at handoff
type: decision
resolves: runtime-F2
---

PLAN.md kickoff prompt restates the rule inline (`[/]` on start, `[x]` on finish, never batch); EXEC.md sets `[/]` BEFORE writing a phase's code + adds a Handoff reconcile gate (Progress must match tree, no work uncommitted while boxes say 'not started'). Placement/enforcement of an existing rule. Provenance: executor left ~137 lines uncommitted, all boxes `[ ]`. Same metadata-drift family as [[D05]].
