---
name: D31
description: token discipline as an always-on core rule: wrap logs, never read whole to learn one bit
type: decision
resolves: runtime-F3,runtime-F4
---

Never read a full log for one bit; wrap it. Ships `tools/summarize_run.py` (command → verdict + notable lines, full log to disk, passes exit code) + `tools/compare_bench.py` (diff two benchmark JSONs on median, flag regressions, exit non-zero). Adds `MODELS.md` (IDs, relative cost, junior-dev executor heuristic, lower-model+higher-effort). Fixes pyproject template PEP 639 license + `Typing :: Typed`. OPEN: run leak check earlier than the tagging gate (EXEC.md collateral or pre-commit hook). See [[D32]].
