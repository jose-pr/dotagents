---
name: D32
description: executor model gated at ready: Executor: line required in every plan
type: decision
resolves: runtime-F4/F5
---

MODELS.md's 'every plan names its executor' was reachable only via load-on-demand, so plans hit `Status: ready` with no model. PLAN.md now requires an `Executor:` line in Structure §3 (after Status) and ready-checklist item 8 gates it (no line ⇒ not ready; needs Opus to execute ⇒ tighten instead). Provenance: this repo's own CLI-package plan. Uses [[D31]]'s MODELS.md.
