# Unlike other repos, this one intentionally tracks the sanitized design log
# (.agents/dotagents/) — CHANGELOG.md cites its D-numbers publicly, so it must
# stay readable. See .agents/AGENTS.md. Plans are private working material and
# live in the private agents repo instead (D56); the private working notes
# .agents/AGENTS.md is likewise untracked.
.agents/plans/
.agents/AGENTS.md
.agents/archive/
CLAUDE.local.md
.claude/

# Python
__pycache__/
*.py[cod]
*.egg-info/
src/*.egg-info/

# dotagents CLI package build artifacts (release artifacts, not committed)
dist/
*.pyz
.venv/
.venv-test/

# OS/editor
.DS_Store
Thumbs.db
.idea/
.vscode/
