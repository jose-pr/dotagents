# Unlike other repos, this one intentionally tracks .agents/ (sanitized design
# log + plans) — see AGENTS.md. Local-only harness artifacts stay ignored.
CLAUDE.local.md
.claude/

# Python
__pycache__/
*.py[cod]
*.egg-info/
src/*.egg-info/

# dotagents CLI package build artifacts (release artifacts, not committed)
dist/
*.pyz
.venv/
.venv-test/

# OS/editor
.DS_Store
Thumbs.db
.idea/
.vscode/
