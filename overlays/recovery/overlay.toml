name = "recovery"
description = "Recover a lost/corrupted ~/.agents from transcripts and caches."
requires = []
routing = [
  """- Reconstruct a lost or corrupted ~/.agents / agent-authored file → $RECOVERY_OVERLAY_ROOT/kb/RECOVERY.md""",
]
