# Private agent config (always excluded — see ~/.agents/flows/REPO.md).
# Root AGENTS.md is NOT excluded: it is the committed, package-shipped public
# library-interface doc for consuming agents.
.agents/
CLAUDE*
.claude

# Build/dist output and generated docs site
build/
dist/
site/

# <!-- EXECUTOR: replace with the language block from kb/<LANG>.md; delete other languages. -->
# Language-specific
<language build/dependency output patterns>

# OS/editor
.DS_Store
Thumbs.db
.idea/
.vscode/
