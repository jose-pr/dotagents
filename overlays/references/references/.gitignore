# Agent/harness tooling (always excluded — see ~/.agents/flows/REPO.md)
# .agents has no trailing slash on purpose: `dotagents link` makes it a symlink,
# which git treats as a file, so a directory-only `.agents/` would NOT ignore it.
AGENTS.md
.agents
CLAUDE*
.claude

# Build/dist output and generated docs site
build/
dist/
site/

# <!-- EXECUTOR: replace with the language block from kb/<LANG>.md; delete other languages. -->
# Language-specific
<language build/dependency output patterns>

# OS/editor
.DS_Store
Thumbs.db
.idea/
.vscode/
