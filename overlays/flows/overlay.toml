name = "flows"
description = "Planning/execution/review/repo workflows + model-selection guidance."
requires = ["references"]
routing = [
  """- Write or revise a plan → $FLOWS_OVERLAY_ROOT/flows/PLAN.md""",
  """- Execute a plan → $FLOWS_OVERLAY_ROOT/flows/EXEC.md""",
  """- Assign a model to a plan / delegate to a subagent / judge if a task is worth an
  expensive model → $FLOWS_OVERLAY_ROOT/MODELS.md""",
  """- Multi-architect plan review (as main or reviewer) → $FLOWS_OVERLAY_ROOT/flows/REVIEW.md""",
  """- Create a repo, bring one to standard, or touch CI/release/docs-site →
  $FLOWS_OVERLAY_ROOT/flows/REPO.md""",
]
