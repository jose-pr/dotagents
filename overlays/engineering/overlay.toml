name = "engineering"
description = "Opinionated always-on engineering discipline: commit shape, release-tag consent, benchmark evidence, token discipline, draft follow-ups."
requires = []
routing = []

# Always-on RULES, not load-on-demand routing: `install` merges each listed
# file's `- **…` bullets into AGENTS.md's managed block under "Always-on rules"
# (D59). Only the leading bullet run is taken, so the prose sections in
# `rules.md` explaining *why* these are not in the base stay out of the core.
rules = [
  "engineering/rules.md",
]
